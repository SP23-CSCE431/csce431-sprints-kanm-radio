<%= form_with(model: officer) do |form| %>
  <% if officer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(officer.errors.count, "error") %> prohibited this officer from being saved:</h2>

      <ul>
        <% officer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :member_id, "Member",style: "display: block" %>
    <%= form.select :member_id, Member.order(:netid).map { |member| [member.firstname + " " + member.lastname, member.id]}, {:prompt => "Select"} , {class: 'form-select'} %>
  </div>

  <div>
    <%= form.label :positionTitle, "Position Title",style: "display: block" %>
    <%= form.text_field :positionTitle, class: 'form-control' %>
  </div>

  <div>
    <%= form.label :picture, "Picture", style: "display: block" %>
    <%= form.text_field :picture, class: 'form-control' %>
  </div>

  <br></br>
  <div>
    <%= form.submit class: "btn btn-primary"  %>
  </div>
<% end %>
