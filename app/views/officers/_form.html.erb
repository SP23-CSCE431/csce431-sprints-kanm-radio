<%= form_with(model: officer) do |form| %>
  <% if officer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(officer.errors.count, "error") %> prohibited this officer from being saved:</h2>

      <ul>
        <% officer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :member_id, style: "display: block" %>
    <%= form.select :member_id, Member.order(:firstname).map {|member| [member.firstname, member.id]}, include_blank:true, id: "id_of_the_book_id_select_box"%>
  </div>

  <div>
    <%= form.label :positionTitle, style: "display: block" %>
    <%= form.text_field :positionTitle %>
  </div>

  <div>
    <%= form.label :picture, style: "display: block" %>
    <%= form.text_field :picture %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
